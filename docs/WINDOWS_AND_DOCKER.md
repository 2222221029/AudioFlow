# Docker Project Notes

This folder is the Docker/Web/NAS project. It uses one container: React/Vite is built into the Python Web/API image.

Run this project with:

```bash
docker compose up -d --build
```

Use `.env` for deployment settings. Keep real cookies and private values out of `.env.example`.
