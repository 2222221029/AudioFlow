# AudioFlow 开发/测试依赖（生产依赖见 requirements.txt）
# 安装: pip install -r requirements-dev.txt -r requirements.txt

# 必须 >=9：tests/ 里的兼容性用例依赖 unittest 的 subTest 能被**报告为失败**。
# pytest 9 内置了 subtest 支持；在 pytest 8 且未安装 pytest-subtests 时，
# subTest 内部的断言失败会被静默忽略，测试会假绿（pytest 9 下会报 SUBFAILED）。
pytest>=9.0.0
pytest-cov>=5.0.0
