# AudioFlow v0.22 依赖
# Python 3.12+ (必须)
# 安装: pip install -r requirements.txt

# HTTP & 网络
requests>=2.31.0
httpx>=0.27.0
urllib3>=2.1.0

# 加密
cryptography>=41.0.0
pycryptodome>=3.19.0

# 解析
beautifulsoup4>=4.12.0
lxml>=4.9.0

qrcode[pil]>=7.4.0  # 蜻蜓FM、网易云听书扫码登录：把登录 URL 渲染成 PNG

# 压缩处理
brotli>=1.0.0

# 工具
PyYAML>=6.0
python-dateutil>=2.8.0
Flask>=3.0.0
waitress>=3.0.0
lark-oapi>=1.4.18  # 飞书 Agent 长连接与交互卡片（交互设计参考 PGZXB/dsh-feishu）
chardet<6
