[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers
markers =
    slow: 耗时较长的测试
    integration: 需要外部服务或真实网络的测试（默认不纳入 CI）
filterwarnings =
    ignore::DeprecationWarning
